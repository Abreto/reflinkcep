type: "query"
patseq:
  type: "lpat" # ast node type
  name: "al" # pattern name
  event: "e" # event type
  cndt:
    expr: name == 1 and price < 4
  loop:
    contiguity: nd-relaxed # relaxed, strict
    from: 2
    to: 3 # inf
context:
  schema:
    e: ["name", "price"]
